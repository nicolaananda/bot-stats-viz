# API Configuration
VITE_API_URL=https://185.201.9.64:3002
VITE_API_TIMEOUT=10000
VITE_API_LOGGING=true

# HTTPS Notes:
# - Use https:// for production
# - Use http:// for local development if needed
# - The app will automatically detect protocol if VITE_API_URL is not set
# - For self-signed certificates, the app will handle them gracefully 